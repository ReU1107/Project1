#pragma once
namespace Engine {

	class HDRLoader final //:public IResourceLoader
	{
	public:

		HDRLoader();

		~HDRLoader();

	public:
		// ì«Ç›çûÇ›Radiance RGBE(HDR)
		//virtual Object * Load(const char * filePath) override;
	};
}